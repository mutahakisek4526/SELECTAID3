<UserControl x:Class="SelectAid.Views.KeyboardLayoutsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Content="Save" Command="{Binding SaveCommand}" />
            <Button Content="Reload" Command="{Binding ReloadCommand}" />
        </StackPanel>
        <TextBox Grid.Row="1" Text="{Binding LayoutJson}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="18" />
    </Grid>
</UserControl>
