<UserControl x:Class="SelectAid.Views.BackupRestoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="20">
        <TextBlock Text="Backup & Restore" FontSize="36" FontWeight="Bold" />
        <StackPanel Orientation="Horizontal">
            <Button Content="Backup" Command="{Binding BackupCommand}" />
            <Button Content="Restore" Command="{Binding RestoreCommand}" />
        </StackPanel>
        <TextBlock Text="{Binding Status}" Margin="0,20,0,0" />
    </StackPanel>
</UserControl>
